[Service]
# Point only fprintd at the patched libfprint. Distro libfprint stays untouched.
# Do not force ELANSPI_ROTATE here: PID 04F3:30C6 (X403FA) needs 180° from the
# driver table. Override later with: sudo ./bin/x403f-fp rotate N
Environment=LD_LIBRARY_PATH=/opt/asus-x403f-fp/lib
