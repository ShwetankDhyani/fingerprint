diff --git a/libfprint/drivers/elanspi.c b/libfprint/drivers/elanspi.c
index 9338013..c563341 100644
--- a/libfprint/drivers/elanspi.c
+++ b/libfprint/drivers/elanspi.c
@@ -80,6 +80,9 @@ struct _FpiDeviceElanSpi
 
   /* wait ctx */
   gint     finger_wait_debounce;
+  gint64   swipe_deadline;
+  gint64   waitup_deadline;
+  gint     capture_kind;
 
   gboolean deactivating, capturing;
 
@@ -97,15 +100,23 @@ elanspi_do_hwreset (FpiDeviceElanSpi *self, GError **err)
   if (g_strcmp0 (g_getenv ("FP_DEVICE_EMULATION"), "1") == 0)
     return;
 
-  /*
-   * TODO: Make this also work with the non-HID cases
-   */
+  /* ASUS VivoBook X403F Windows INF uses GPIO reset (ResetType=2), not
+   * touchpad-PTP HID reset. HID feature 0x0e still works on many units,
+   * but failing it must not abort init — SW reset is enough. */
+  (void) err;
 
-  int fd = open ((char *) fpi_device_get_udev_data (FP_DEVICE (self), FPI_DEVICE_UDEV_SUBTYPE_HIDRAW), O_RDWR);
+  const char *path = (char *) fpi_device_get_udev_data (FP_DEVICE (self), FPI_DEVICE_UDEV_SUBTYPE_HIDRAW);
+  if (!path)
+    {
+      fp_warn ("no hidraw path; skipping HID reset");
+      return;
+    }
+
+  int fd = open (path, O_RDWR);
 
   if (fd < 0)
     {
-      g_set_error (err, G_IO_ERROR, g_io_error_from_errno (errno), "unable to open hid");
+      fp_warn ("unable to open hid (%s); continuing with SW reset", g_strerror (errno));
       return;
     }
 
@@ -114,12 +125,8 @@ elanspi_do_hwreset (FpiDeviceElanSpi *self, GError **err)
   };
 
   if (ioctl (fd, HIDIOCSFEATURE (5), &buf) != 5)
-    {
-      g_set_error (err, G_IO_ERROR, g_io_error_from_errno (errno), "unable to reset via hid");
-      goto out;
-    }
+    fp_warn ("HID feature reset failed (%s); continuing with SW reset", g_strerror (errno));
 
-out:
   close (fd);
 }
 
@@ -234,6 +241,12 @@ enum elanspi_fp_capture_state {
   ELANSPI_FPCAPT_NSTATES
 };
 
+enum elanspi_capture_kind {
+  ELANSPI_KIND_WAITDOWN = 0,
+  ELANSPI_KIND_FINGER,
+  ELANSPI_KIND_WAITUP,
+};
+
 /* helpers */
 
 static FpiSpiTransfer *
@@ -457,6 +470,21 @@ elanspi_capture_old_line_handler (FpiSpiTransfer *transfer, FpDevice *dev, gpoin
     }
 }
 
+static void
+elanspi_handle_line_timeout (FpiSsm *ssm, FpiDeviceElanSpi *self)
+{
+  fp_warn ("x403f-waitup: line timeout kind=%d", self->capture_kind);
+  if (self->capture_kind == ELANSPI_KIND_WAITUP)
+    {
+      /* Drain finished (or sensor went quiet). Parent will release finger. */
+      fpi_ssm_mark_completed (ssm);
+      return;
+    }
+  /* Re-issue 0x01 instead of failing the enroll session. WRITE_CAPTURE is 0
+   * for both the old-line and HV capture SSMs. */
+  fpi_ssm_jump_to_state (ssm, 0);
+}
+
 static void
 elanspi_capture_old_handler (FpiSsm *ssm, FpDevice *dev)
 {
@@ -487,8 +515,7 @@ elanspi_capture_old_handler (FpiSsm *ssm, FpDevice *dev)
           /* has the timeout expired? -- disabled in testing since valgrind is very slow */
           if (g_get_monotonic_time () > self->capture_timeout && g_strcmp0 (g_getenv ("FP_DEVICE_EMULATION"), "1") != 0)
             {
-              /* end with a timeout */
-              fpi_ssm_mark_failed (ssm, g_error_new (G_IO_ERROR, G_IO_ERROR_TIMED_OUT, "timed out waiting for new line"));
+              elanspi_handle_line_timeout (ssm, self);
               return;
             }
           /* check again */
@@ -765,8 +792,7 @@ elanspi_capture_hv_handler (FpiSsm *ssm, FpDevice *dev)
           /* has the timeout expired? */
           if (g_get_monotonic_time () > self->capture_timeout)
             {
-              /* end with a timeout */
-              fpi_ssm_mark_failed (ssm, g_error_new (G_IO_ERROR, G_IO_ERROR_TIMED_OUT, "timed out waiting for image"));
+              elanspi_handle_line_timeout (ssm, self);
               return;
             }
           /* check again */
@@ -1171,10 +1197,25 @@ elanspi_correct_with_bg (FpiDeviceElanSpi *self, guint16 *raw_image)
   return count;
 }
 
+static int
+elanspi_get_rotation (FpiDeviceElanSpi *self)
+{
+  const gchar *env = g_getenv ("ELANSPI_ROTATE");
+
+  if (env && env[0] != '\0')
+    {
+      int v = g_ascii_strtoll (env, NULL, 10);
+      if (v >= 0 && v <= 3)
+        return v;
+    }
+
+  return fpi_device_get_driver_data (FP_DEVICE (self)) & 3;
+}
+
 static guint16
 elanspi_lookup_pixel_with_rotation (FpiDeviceElanSpi *self, const guint16 *data_in, int y, int x)
 {
-  int rotation = fpi_device_get_driver_data (FP_DEVICE (self)) & 3;
+  int rotation = elanspi_get_rotation (self);
   gint x1 = x, y1 = y;
 
   if (rotation == ELANSPI_180_ROTATE)
@@ -1329,11 +1370,50 @@ elanspi_fp_assembling_get_pixel (struct fpi_frame_asmbl_ctx *ctx, struct fpi_fra
   return frame->data[y * ctx->frame_width + x];
 }
 
+/* Separable gaussian smoothing (sigma ~1.2). Suppresses sensor-noise speckle
+ * that otherwise produces spurious, non-repeatable image features. */
+static void
+elanspi_smooth_image (FpImage *img)
+{
+  static const guint kern[7] = { 34, 111, 218, 298, 218, 111, 34 };
+  guint w = img->width, h = img->height;
+  g_autofree guint8 *tmp = g_malloc (w * h);
+  guint8 *data = (guint8 *) img->data;
+
+  for (guint y = 0; y < h; y++)
+    for (guint x = 0; x < w; x++)
+      {
+        guint acc = 0, wsum = 0;
+        for (gint k = -3; k <= 3; k++)
+          {
+            gint xx = (gint) x + k;
+            if (xx < 0 || xx >= (gint) w)
+              continue;
+            acc += kern[k + 3] * data[y * w + xx];
+            wsum += kern[k + 3];
+          }
+        tmp[y * w + x] = acc / wsum;
+      }
+  for (guint y = 0; y < h; y++)
+    for (guint x = 0; x < w; x++)
+      {
+        guint acc = 0, wsum = 0;
+        for (gint k = -3; k <= 3; k++)
+          {
+            gint yy = (gint) y + k;
+            if (yy < 0 || yy >= (gint) h)
+              continue;
+            acc += kern[k + 3] * tmp[yy * w + x];
+            wsum += kern[k + 3];
+          }
+        data[y * w + x] = acc / wsum;
+      }
+}
+
 static void
 elanspi_fp_frame_stitch_and_submit (FpiDeviceElanSpi *self)
 {
   g_autoptr(FpImage) img = NULL;
-  g_autoptr(FpImage) scaled = NULL;
   struct fpi_frame_asmbl_ctx assembling_ctx = {
     .image_width = (self->frame_width * 3) / 2,
 
@@ -1348,12 +1428,15 @@ elanspi_fp_frame_stitch_and_submit (FpiDeviceElanSpi *self)
 
   fpi_do_movement_estimation (&assembling_ctx, frame_start);
   img = fpi_assemble_frames (&assembling_ctx, frame_start);
-  scaled = fpi_image_resize (img, 2, 2);
 
-  scaled->flags |= FPI_IMAGE_PARTIAL | FPI_IMAGE_COLORS_INVERTED;
+  /* Native scale (~10 px ridge period) is already in SIGFM's expected band.
+   * 2x upscale starves feature extraction; FPI_IMAGE_PARTIAL culls most
+   * minutiae on these narrow strips. */
+  elanspi_smooth_image (img);
+  img->flags |= FPI_IMAGE_COLORS_INVERTED;
 
   /* submit image */
-  fpi_image_device_image_captured (FP_IMAGE_DEVICE (self), g_steal_pointer (&scaled));
+  fpi_image_device_image_captured (FP_IMAGE_DEVICE (self), g_steal_pointer (&img));
 
   /* clean out frame data */
   g_slist_free_full (g_steal_pointer (&self->fp_frame_list), g_free);
@@ -1387,6 +1470,24 @@ elanspi_fp_frame_handler (FpiSsm *ssm, FpiDeviceElanSpi *self)
 {
   g_autofree struct fpi_frame *this_frame = NULL;
 
+  if (self->swipe_deadline != 0 &&
+      g_get_monotonic_time () > self->swipe_deadline)
+    {
+      self->swipe_deadline = 0;
+      if (g_slist_length (self->fp_frame_list) >= ELANSPI_MIN_FRAMES_SWIPE)
+        {
+          fp_dbg ("<fp_frame> press timeout, submitting %u frames",
+                  g_slist_length (self->fp_frame_list));
+          elanspi_fp_frame_stitch_and_submit (self);
+        }
+      else
+        {
+          fp_dbg ("<fp_frame> press timeout, too few frames");
+          fpi_image_device_retry_scan (FP_IMAGE_DEVICE (self), FP_DEVICE_RETRY_TOO_SHORT);
+        }
+      goto finish_capture;
+    }
+
   switch (elanspi_guess_image (self, self->last_image))
     {
     case ELANSPI_GUESS_UNKNOWN:
@@ -1464,8 +1565,11 @@ elanspi_fp_frame_handler (FpiSsm *ssm, FpiDeviceElanSpi *self)
   return;
 
 finish_capture:
-  /* prepare for wait up */
+  /* Drain leftover SPI lines. Skipping wait-up left the sensor mid-frame,
+   * so the next wait-down never saw a press after enroll-stage-passed. */
   self->finger_wait_debounce = 0;
+  self->waitup_deadline = g_get_monotonic_time () + ELANSPI_WAITUP_TIMEOUT_USEC;
+  fp_warn ("x403f-waitup: draining after press");
   fpi_ssm_jump_to_state (ssm, ELANSPI_FPCAPT_WAITUP_CAPTURE);
   return;
 
@@ -1481,7 +1585,10 @@ elanspi_fp_capture_ssm_handler (FpiSsm *ssm, FpDevice *dev)
     {
     case ELANSPI_FPCAPT_INIT:
       self->finger_wait_debounce = 0;
-
+      self->swipe_deadline = 0;
+      self->waitup_deadline = 0;
+      self->capture_kind = ELANSPI_KIND_WAITDOWN;
+      fp_warn ("x403f-waitup: waiting for press");
       fpi_ssm_next_state (ssm);
       return;
 
@@ -1501,6 +1608,44 @@ elanspi_fp_capture_ssm_handler (FpiSsm *ssm, FpDevice *dev)
 
           return;
         }
+      switch (fpi_ssm_get_cur_state (ssm))
+        {
+        case ELANSPI_FPCAPT_WAITDOWN_CAPTURE:
+          self->capture_kind = ELANSPI_KIND_WAITDOWN;
+          break;
+        case ELANSPI_FPCAPT_WAITUP_CAPTURE:
+          self->capture_kind = ELANSPI_KIND_WAITUP;
+          if (self->waitup_deadline != 0 &&
+              g_get_monotonic_time () > self->waitup_deadline)
+            {
+              fp_warn ("x403f-waitup: drain timeout, releasing finger");
+              self->capturing = FALSE;
+              self->waitup_deadline = 0;
+              fpi_image_device_report_finger_status (FP_IMAGE_DEVICE (self), FALSE);
+              fpi_ssm_mark_completed (ssm);
+              return;
+            }
+          break;
+        case ELANSPI_FPCAPT_FP_CAPTURE:
+          self->capture_kind = ELANSPI_KIND_FINGER;
+          if (self->swipe_deadline != 0 &&
+              g_get_monotonic_time () > self->swipe_deadline)
+            {
+              self->swipe_deadline = 0;
+              if (g_slist_length (self->fp_frame_list) >= ELANSPI_MIN_FRAMES_SWIPE)
+                elanspi_fp_frame_stitch_and_submit (self);
+              else
+                fpi_image_device_retry_scan (FP_IMAGE_DEVICE (self), FP_DEVICE_RETRY_TOO_SHORT);
+              self->finger_wait_debounce = 0;
+              self->waitup_deadline = g_get_monotonic_time () + ELANSPI_WAITUP_TIMEOUT_USEC;
+              fp_warn ("x403f-waitup: draining after press");
+              fpi_ssm_jump_to_state (ssm, ELANSPI_FPCAPT_WAITUP_CAPTURE);
+              return;
+            }
+          break;
+        default:
+          break;
+        }
       /* if sensor is hv */
       if (self->sensor_id == 0xe)
         chld = fpi_ssm_new (dev, elanspi_capture_hv_handler, ELANSPI_CAPTHV_NSTATES);
@@ -1525,6 +1670,7 @@ elanspi_fp_capture_ssm_handler (FpiSsm *ssm, FpDevice *dev)
 
       /* report finger status */
       fpi_image_device_report_finger_status (FP_IMAGE_DEVICE (self), TRUE);
+      self->swipe_deadline = g_get_monotonic_time () + ELANSPI_SWIPE_TIMEOUT_USEC;
 
       /* jump */
       fpi_ssm_jump_to_state (ssm, ELANSPI_FPCAPT_FP_CAPTURE);
@@ -1535,19 +1681,25 @@ elanspi_fp_capture_ssm_handler (FpiSsm *ssm, FpDevice *dev)
       return;
 
     case ELANSPI_FPCAPT_WAITUP_PROCESS:
+      if (self->waitup_deadline != 0 &&
+          g_get_monotonic_time () > self->waitup_deadline)
+        {
+          fp_warn ("x403f-waitup: drain timeout, releasing finger");
+          self->capturing = FALSE;
+          self->waitup_deadline = 0;
+          fpi_image_device_report_finger_status (FP_IMAGE_DEVICE (self), FALSE);
+          fpi_ssm_mark_completed (ssm);
+          return;
+        }
       if (!elanspi_check_waitupdown_done (self, ELANSPI_GUESS_EMPTY))
         {
-          /* take another image */
           fpi_ssm_jump_to_state (ssm, ELANSPI_FPCAPT_WAITUP_CAPTURE);
           return;
         }
-
-      /* Immediately set capturing to FALSE so that when report_finger_status tries to re-start
-       * capturing in enroll we don't hit the assert since the old SSM is about to stop. */
+      fp_warn ("x403f-waitup: finger lifted");
       self->capturing = FALSE;
+      self->waitup_deadline = 0;
       fpi_image_device_report_finger_status (FP_IMAGE_DEVICE (self), FALSE);
-
-      /* finish */
       fpi_ssm_mark_completed (ssm);
       return;
     }
@@ -1639,9 +1791,37 @@ elanspi_fp_capture_finish (FpiSsm *ssm, FpDevice *dev, GError *error)
       return;
     }
 
-  /* if there was an error, report it */
   if (error)
-    fpi_image_device_session_error (idev, error);
+    {
+      if (g_error_matches (error, G_IO_ERROR, G_IO_ERROR_TIMED_OUT))
+        {
+          gint kind = self->capture_kind;
+          fp_warn ("x403f-waitup: capture SSM timeout kind=%d", kind);
+          g_error_free (error);
+          if (kind == ELANSPI_KIND_FINGER)
+            {
+              /* Valid from CAPTURE: ask for another press. */
+              fpi_image_device_retry_scan (idev, FP_DEVICE_RETRY_GENERAL);
+              fpi_image_device_report_finger_status (idev, FALSE);
+              return;
+            }
+          if (kind == ELANSPI_KIND_WAITUP)
+            {
+              /* Finger-off lets enroll start the next AWAIT_FINGER_ON. */
+              fpi_image_device_report_finger_status (idev, FALSE);
+              return;
+            }
+          /* Wait-down is still AWAIT_FINGER_ON. retry_scan is not a legal
+           * transition from that state and froze enroll after stage 1. */
+          self->capturing = TRUE;
+          fpi_ssm_start (fpi_ssm_new (dev,
+                                      elanspi_fp_capture_ssm_handler,
+                                      ELANSPI_FPCAPT_NSTATES),
+                         elanspi_fp_capture_finish);
+          return;
+        }
+      fpi_image_device_session_error (idev, error);
+    }
 }
 
 static void
@@ -1699,9 +1879,16 @@ fpi_device_elanspi_class_init (FpiDeviceElanSpiClass *klass)
   dev_class->type = FP_DEVICE_TYPE_UDEV;
   dev_class->id_table = elanspi_id_table;
   dev_class->scan_type = FP_SCAN_TYPE_SWIPE;
-  dev_class->nr_enroll_stages = 7;       /* these sensors are very hit or miss, may as well record a few extras */
-
-  img_class->bz3_threshold = 24;
+  /* Eight stages finish before the reader sits armed for minutes. */
+  dev_class->nr_enroll_stages = 8;
+  /* Tiny ACPI SPI sensor; the default 180s software thermal model is wrong
+   * and turned a stuck wait-up into enroll-disconnected. */
+  dev_class->temp_hot_seconds = -1;
+
+  /* SIGFM (SIFT) matching: NBIS/bozorth3 cannot discriminate fingers on the
+   * narrow stitched strips these sensors produce. */
+  img_class->algorithm = FPI_DEVICE_ALGO_SIGFM;
+  img_class->bz3_threshold = 100;
   img_class->img_open = elanspi_open;
   img_class->activate = elanspi_activate;
   img_class->deactivate = elanspi_deactivate;
diff --git a/libfprint/drivers/elanspi.h b/libfprint/drivers/elanspi.h
index a8d7319..8440c8a 100644
--- a/libfprint/drivers/elanspi.h
+++ b/libfprint/drivers/elanspi.h
@@ -339,7 +339,12 @@ static const struct elanspi_regtable elanspi_calibration_table_new_page1 = {
 #define ELANSPI_TP_VID 0x04f3
 
 // using checkargs ACPI:HIDPID
+/* pid 0 = match any ELAN HID companion (see fp-context.c wildcard).
+ * Specific PIDs come first so rotation metadata is used when known.
+ * 0x3128 is the ASUS VivoBook 14 X403F / X403FA touchpad companion. */
 static const FpIdEntry elanspi_id_table[] = {
+  {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x3128}, .driver_data = ELANSPI_NO_ROTATE},
+  {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7002", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x3128}, .driver_data = ELANSPI_NO_ROTATE},
   {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x3057}, .driver_data = ELANSPI_180_ROTATE},
   {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x3087}, .driver_data = ELANSPI_180_ROTATE},
   {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x30c6}, .driver_data = ELANSPI_180_ROTATE},
@@ -349,6 +354,16 @@ static const FpIdEntry elanspi_id_table[] = {
   {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN70A1", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x30b2}, .driver_data = ELANSPI_NO_ROTATE},
   {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x309f}, .driver_data = ELANSPI_180_ROTATE},
   {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x241f}, .driver_data = ELANSPI_NO_ROTATE},
+  {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x2766}, .driver_data = ELANSPI_NO_ROTATE},
+  {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x3104}, .driver_data = ELANSPI_90RIGHT_ROTATE},
+  {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x3028}, .driver_data = ELANSPI_90LEFT_ROTATE},
+  {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x304d}, .driver_data = ELANSPI_90LEFT_ROTATE},
+  {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x3059}, .driver_data = ELANSPI_90LEFT_ROTATE},
+  {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x306f}, .driver_data = ELANSPI_90LEFT_ROTATE},
+  {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x22fe}, .driver_data = ELANSPI_90LEFT_ROTATE},
+  {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7001", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x0000}, .driver_data = ELANSPI_NO_ROTATE},
+  {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN7002", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x0000}, .driver_data = ELANSPI_NO_ROTATE},
+  {.udev_types = ELANSPI_UDEV_TYPES, .spi_acpi_id = "ELAN70A1", .hid_id = {.vid = ELANSPI_TP_VID, .pid = 0x0000}, .driver_data = ELANSPI_NO_ROTATE},
   {.udev_types = 0}
 };
 
@@ -359,24 +374,31 @@ static const FpIdEntry elanspi_id_table[] = {
 
 #define ELANSPI_HV_CALIBRATION_TARGET_MEAN 3000
 
-#define ELANSPI_MIN_EMPTY_INVALID_PERCENT 6
+#define ELANSPI_MIN_EMPTY_INVALID_PERCENT 2
 #define ELANSPI_MAX_REAL_INVALID_PERCENT 3
 
-#define ELANSPI_MIN_REAL_STDDEV (592 * 592)
-#define ELANSPI_MAX_EMPTY_STDDEV (350 * 350)
+#define ELANSPI_MIN_REAL_STDDEV (400 * 400)
+#define ELANSPI_MAX_EMPTY_STDDEV (800 * 800)
 
-#define ELANSPI_MIN_FRAMES_DEBOUNCE 2
+#define ELANSPI_MIN_FRAMES_DEBOUNCE 1
 
-#define ELANSPI_SWIPE_FRAMES_DISCARD 1
-#define ELANSPI_MIN_FRAMES_SWIPE (7 + ELANSPI_SWIPE_FRAMES_DISCARD)
-#define ELANSPI_MAX_FRAMES_SWIPE (20 + ELANSPI_SWIPE_FRAMES_DISCARD)
+#define ELANSPI_SWIPE_FRAMES_DISCARD 0
+#define ELANSPI_MIN_FRAMES_SWIPE 2
+#define ELANSPI_MAX_FRAMES_SWIPE 20
 
 #define ELANSPI_MAX_FRAME_HEIGHT 43
-#define ELANSPI_MIN_FRAME_TO_FRAME_DIFF (250 * 250)
+#define ELANSPI_MIN_FRAME_TO_FRAME_DIFF (80 * 80)
 
 #define ELANSPI_HV_SENSOR_FRAME_DELAY 23
 
-#define ELANSPI_OTP_TIMEOUT_USEC (12 * 1000)
+/* A press-and-hold on this corner pad produces few frames. Stop capturing
+ * after this many microseconds and submit whatever we have. */
+#define ELANSPI_SWIPE_TIMEOUT_USEC (2500 * 1000)
+/* Drain leftover SPI lines after a press, then force finger-off. Skipping
+ * wait-up left the sensor mid-frame so stage 2 never saw a press. */
+#define ELANSPI_WAITUP_TIMEOUT_USEC (800 * 1000)
 
-#define ELANSPI_OLD_CAPTURE_TIMEOUT_USEC (100 * 1000)
-#define ELANSPI_HV_CAPTURE_TIMEOUT_USEC (50 * 1000)
+#define ELANSPI_OTP_TIMEOUT_USEC (2000 * 1000)
+
+#define ELANSPI_OLD_CAPTURE_TIMEOUT_USEC (2000 * 1000)
+#define ELANSPI_HV_CAPTURE_TIMEOUT_USEC (1000 * 1000)
diff --git a/libfprint/fp-context.c b/libfprint/fp-context.c
index 34fcdda..f337182 100644
--- a/libfprint/fp-context.c
+++ b/libfprint/fp-context.c
@@ -529,7 +529,7 @@ fp_context_enumerate (FpContext *context)
                     if (sscanf (hid_id, "%*X:%X:%X", &vendor, &product) != 2)
                       continue;
 
-                    if (vendor == entry->hid_id.vid && product == entry->hid_id.pid)
+                    if (vendor == entry->hid_id.vid && (entry->hid_id.pid == 0 || product == entry->hid_id.pid))
                       break;
                   }
                 /* If match was not found exit */
diff --git a/libfprint/fp-image.c b/libfprint/fp-image.c
index 2303f6a..b36564c 100644
--- a/libfprint/fp-image.c
+++ b/libfprint/fp-image.c
@@ -316,7 +316,7 @@ fp_image_sigfm_extract_thread_func (GTask * task, void * src_obj,
   g_timer_stop (timer);
   fp_dbg ("sigfm extract completed in %f secs", g_timer_elapsed (timer, NULL));
   g_timer_destroy (timer);
-  if (sigfm_keypoints_count (data->sigfm_info) < 25)
+  if (sigfm_keypoints_count (data->sigfm_info) < 8)
     {
       g_task_return_new_error (task, G_IO_ERROR, G_IO_ERROR_FAILED,
                                "No enough keypoints found");
